name: Workflow Demo

on: 
  - push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Create a text file
      run: echo "Hello, world!" >> hello.txt

    - name: Archive text file
      uses: actions/upload-artifact@v4
      with:
        name: hello-artifact
        path: hello.txt
  
  download:
      permissions:
        contents: write
      runs-on: ubuntu-latest
      needs:
        - build
      
      steps:
        - name: Get the artifacts
          uses: actions/download-artifact@v4 
          with:
            name: hello-artifact
            path: ./artifacts

        - name: Display
          run: cat ./artifacts/hello.txt
        
        # - name: Checkout
        #   uses: actions/checkout@v4
        
        # - name: PushIt
        #   run: |
        #     git config --global user.name "rlc2000"
        #     git config --global user.email "rasham.chhetri@gmail.com"
        #     git pull
        #     git add ./artifacts/hello.txt
        #     git commit -m "artifact"
        #     git push
